import { ProfilePictureEntry } from "src/profile-picture/entities/profile-picture.entity";
import { ProfilePictureService } from "src/profile-picture/profile-picture.service";
import { Role } from "src/users/Role";

export class MockEntryService extends ProfilePictureService
{
    // async saveImage(profilePhoto: string, userId: string) {
    //     // return { };
    // }
}